# Cutover T0 Smoke Report (VM)

- generated_at_local: 2026-04-05 12:00:29
- target_vm: Heliora-VM
- remote_backend_dir: /home/heliora/heliora_backend
- objective: execute T0 cutover to postgres and validate post-cutover smoke with rigorous evidence

## 1. Configuration Cutover

1. Backed up remote env file:
   - `.env.bak.20260405_115819`
2. Updated remote runtime setting:
   - `TASK_PERSISTENCE_BACKEND=postgres`
3. Verified key env lines on VM:
   - `TASK_QUEUE_BACKEND=rabbitmq`
   - `DATABASE_URL=postgresql://heliora:heliora521@127.0.0.1:5432/heliora`
   - `TASK_PERSISTENCE_BACKEND=postgres`

## 2. Dependency and Schema Readiness

1. Executed VM SQL pack with schema keep enabled:
   - `KEEP_SCHEMA=true bash scripts/verify_task_persistence_pg_vm.sh`
2. Result:
   - PASS (`[5/5] done`)
   - `Schema kept on VM (KEEP_SCHEMA=true).`

## 3. Runtime Restart

1. Executed:
   - `bash scripts/stop_api.sh || true`
   - `bash scripts/start_api_bg.sh`
2. Result:
   - API started and `/health` returned `OK`
   - worker started (later temporarily stopped for deterministic cancel smoke and then restored)

## 4. Post-Cutover Endpoint Smoke (submit/status/events/cancel)

1. Deterministic smoke (worker temporarily stopped to avoid cancel race):
   - health check: PASS
   - memory retrieve endpoint: PASS
   - task-1: submit -> status -> events -> consume-next -> status/events: PASS
   - task-2: submit -> cancel -> status/events: PASS
2. Runtime evidence:
   - `smoke_ok=true`
   - `task1=task_f21b271313 status_before=queued status_after=completed`
   - `task2=task_1a8668e615 status=canceled events=['created', 'routed', 'queued', 'canceled']`
   - `consume_code=OK consume_status=completed`

## 5. Queue Path Validation (memory/rabbitmq)

1. postgres + memory profile:
   - `TASK_PERSISTENCE_BACKEND=postgres TASK_QUEUE_BACKEND=memory .venv/bin/python -m pytest tests/test_tasks_submit.py -q -rA`
   - result: `27 passed`
2. postgres + rabbitmq profile:
   - `TASK_PERSISTENCE_BACKEND=postgres TASK_QUEUE_BACKEND=rabbitmq RABBITMQ_E2E_REQUIRED=true .venv/bin/python -m pytest tests/test_tasks_rabbitmq_e2e.py -q -rA`
   - result: `1 passed`

## 6. Final Service Health

1. VM runtime check:
   - `/health` returned `{"code":"OK","data":{"status":"healthy"}}`
   - `worker=running pid=8968`

## 7. Related Cloud CI Evidence

1. run#4 (success):
   - `https://github.com/InfiniX520/Heliora/actions/runs/23993634248`
2. run#4 artifact:
   - `https://github.com/InfiniX520/Heliora/actions/runs/23993634248/artifacts/6274898630`

## 8. Cutover Decision

- rollback_triggered: no
- decision: keep postgres in runtime
- next: keep 15-minute+ observation window and complete release sign-off record
