# 接口口径统一

1. 统一前缀: /api/v1。
2. 所有响应必须携带 trace_id。
3. 记忆相关接口统一归属 /api/v1/memory/*。
4. 历史路径 /api/memory/* 仅作为兼容别名，网关内部转发到 /api/v1/memory/*。
5. 记忆写入前必须完成 evidence 校验，evidence 为空直接拒绝。
6. 默认模式为 strict；trusted_local_max 必须显式开启并记录审计事件。
7. trusted_local_max 仅用于本地部署，不作为远程默认模式。