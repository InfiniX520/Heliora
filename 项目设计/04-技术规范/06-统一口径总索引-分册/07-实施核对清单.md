# 实施核对清单

1. 配置层: MEMORY_* 配置项与默认值已对齐。
2. 数据层: memory_records/memory_evidence/memory_links/memory_events/memory_feedback 已建模。
3. 服务层: gate/retriever/conflict/consolidator 分层实现到位。
4. 接口层: memory/retrieve、feedback、rollback、delete 已落API契约。
5. 观测层: 删除SLA、冲突时长、Recall@K/Precision@K 指标已接入监控。
6. 交互层: 冲突面板与删除进度面板已提供可视化入口。
7. 安全层: strict/trusted_local_max 模式切换行为与审计链路已验证。