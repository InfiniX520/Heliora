# Heliora(曦澪) 当前测试清单

测试目标只有一个：保证主流程能跑通，失败时能定位。

---

## 1. 当前必测范围

只测 7 条主流程：
1. 插件发送错误上下文。
2. 后端接收并保存错误记录。
3. 系统返回引导式定位与下一步建议。
4. 生成一次答辩演练反馈。
5. 文件整理执行后可回滚。
6. 记忆写入门控与检索命中。
7. 记忆冲突处理与删除SLA闭环。

---

## 2. 每条流程的通过标准

### 2.0 记忆专项量化门槛（与最终方案对齐）

除流程跑通外，记忆系统需满足以下硬门槛：
1. Recall@K >= 0.85。
2. Precision@K >= 0.80。
3. Conflict Resolution Accuracy >= 0.90。
4. Hallucinated Memory Rate <= 2%。
5. 对话首字延迟增量 P95 <= 120ms。
6. 删除请求闭环SLA <= 5分钟。
7. 单条冲突未决时长 <= 24小时。

说明：
1. P0 阶段可先做小样本验证，但必须保留指标采集与报表出口。
2. P1 起必须覆盖图谱扩展召回路径（Neo4j + PPR）验证。

### 用例1：插件发送错误
1. 输入：错误信息 + 文件路径 + 代码片段。
2. 通过：后端返回成功状态与 task_id。

### 用例2：错误记录落库
1. 输入：来自用例1的请求。
2. 通过：数据库存在对应记录，字段完整。

### 用例3：引导式纠错
1. 输入：真实报错样例。
2. 通过：返回结构包含结论、依据、下一步。

### 用例4：答辩演练
1. 输入：项目上下文。
2. 通过：生成 10 题与结构化反馈。

### 用例5：文件管家回滚
1. 输入：一个测试目录。
2. 通过：执行整理后可一键恢复到原状态。

### 用例6：长期记忆写入与检索
1. 输入：包含明确偏好的多轮对话。
2. 通过：产生含 evidence 的记忆记录，并在后续对话命中。

### 用例7：冲突与删除闭环
1. 输入：同一偏好给出相反新证据 + 删除请求。
2. 通过：冲突进入五区间决策流程；删除请求5分钟内完成软删与索引清除。

---

## 3. 每次开发后的固定动作

1. 跑完 7 条主流程。
2. 记录失败用例与日志位置。
3. 修复后重跑失败用例。
4. 全部通过后再进入下一任务。

---

## 3.1 CI 门禁（新增）

1. RabbitMQ E2E 已纳入 CI 强制门禁，路径：`.github/workflows/backend-rabbitmq-gate.yml`。
2. 门禁必须通过 `tests/test_tasks_rabbitmq_e2e.py`，不允许以 skip 通过。
3. CI 环境使用 `RABBITMQ_E2E_REQUIRED=true`；若 RabbitMQ 不可用，测试直接失败。
4. CI 增加 Ruff 静态检查门禁：`python -m ruff check app tests`。
5. CI 增加 Mypy 类型检查门禁：`python -m mypy app`。
6. CI 增加覆盖率门禁：`python -m pytest --cov=app --cov-report=term-missing --cov-fail-under=70 -q`。
7. 任一门禁失败即阻断合并。
8. CI 新增 PostgreSQL 冒烟门禁：`TASK_PERSISTENCE_BACKEND=postgres` + `TASK_QUEUE_BACKEND=memory`，执行 `001_up -> tests/test_tasks_submit.py -> 001_down`。
9. CI 新增 RabbitMQ + PostgreSQL 组合门禁：`TASK_PERSISTENCE_BACKEND=postgres` + `TASK_QUEUE_BACKEND=rabbitmq`，执行 `001_up -> tests/test_tasks_rabbitmq_e2e.py -> 001_down`。
10. RabbitMQ E2E 用例已补充持久层断言，强制校验 `task_state_store/task_event_store` 写入成功，避免仅内存路径通过。
11. CI 新增环境一致性门禁：`python scripts/validate_env_consistency.py --env-file .env`，用于阻断 DSN/账号/密码漂移。

## 3.2 Day-4.2 实测记录（2026-04-04）

1. VM 配置 A（postgres + memory）执行 `tests/test_tasks_submit.py`：27/27 通过。
2. VM 配置 B（postgres + rabbitmq）执行 `tests/test_tasks_rabbitmq_e2e.py`：1/1 通过。
3. 两组均按 `001_up -> pytest -> 001_down` 流程执行并完成清理。

## 3.3 Day-4.2 一键矩阵回归（VM）

推荐命令：

```bash
bash scripts/run_task_persistence_pg_matrix_vm.sh
```

通过标准：
1. profile A（postgres + memory）输出 `tests/test_tasks_submit.py` 全通过。
2. profile B（postgres + rabbitmq）输出 `tests/test_tasks_rabbitmq_e2e.py` 全通过。
3. 两个 profile 均完成 `001_up -> pytest -> 001_down` 自动清理。
4. 执行前必须通过 `.env` 一致性预检（`scripts/validate_env_consistency.py`）。

## 3.4 Day-4.3 迁移与回滚演练（VM）

推荐命令：

```bash
bash scripts/rehearse_task_persistence_cutover_rollback_vm.sh
```

通过标准：
1. postgres 切换阶段通过矩阵回归（`tests/test_tasks_submit.py` 与 `tests/test_tasks_rabbitmq_e2e.py`）。
2. sqlite 回滚阶段通过 `tests/test_tasks_submit.py`。
3. 脚本输出最终状态 `cutover and rollback rehearsal passed`。

## 3.5 Day-4.3 CI 报告固化

通过标准：
1. workflow 产出 `.ci-reports/` 下的 JUnit XML 与 coverage XML。
2. workflow 上传 artifact `backend-rabbitmq-gate-reports`。
3. workflow 在 GitHub Step Summary 输出门禁结果表（env/ruff/mypy/postgres smoke/rabbitmq/coverage）。
4. PR 评审记录中必须附上对应 workflow 链接与 artifact 链接。

## 3.6 Day-4.4 VM 脚本可移植性校验

通过标准：
1. `scripts/verify_task_persistence_pg_vm.sh` 不包含宿主机绝对 SSH 路径默认值。
2. `scripts/run_task_persistence_pg_matrix_vm.sh` 不包含宿主机绝对 SSH 路径默认值。
3. `scripts/rehearse_task_persistence_cutover_rollback_vm.sh` 不包含宿主机绝对 SSH 路径默认值。
4. 三个脚本均支持 `SSH_BIN` 显式覆盖，并可在未覆盖时自动发现 SSH 可执行文件。
5. 宿主机执行 `bash -n` 语法校验全部通过。

## 3.7 Day-4.4 ANN 类型注解治理校验

通过标准：
1. `ruff check app tests scripts --select ANN` 全通过。
2. `ruff check app tests` 全通过。
3. `mypy app` 全通过。
4. 本批次修复不改变既有业务逻辑，仅补充类型注解与测试辅助函数签名约束。

## 3.8 Day-4.4 CI ANN 审计接入校验

通过标准：
1. workflow `backend-rabbitmq-gate.yml` 新增 ANN 审计步骤（`ruff check app tests scripts --select ANN`）。
2. ANN 审计步骤为非阻塞（continue-on-error），不影响主门禁结果。
3. ANN 审计输出落盘到 `.ci-reports/ann_audit.txt`，并随 artifact 一并上传。
4. GitHub Step Summary 包含 `ann audit (non-blocking)` 结果行。

## 3.9 Day-4.4 发布前一键预检脚本校验

通过标准：
1. `scripts/preflight_release_window_vm.sh` 可正常执行并生成 `.release-reports/release_preflight_*.md` 报告。
2. 脚本支持 `--skip-sql-review --skip-matrix --skip-rehearsal` 轻量模式，用于快速验证执行器可用性。
3. 全量模式下按顺序执行：env consistency -> vm sql review -> vm runtime matrix -> vm cutover rollback rehearsal。
4. 任一步骤失败时脚本返回非零退出码，且报告中保留失败步骤与耗时。

---

## 4. 当前不做的测试

1. 大规模压测。
2. 云端生产环境测试。
3. 复杂自动化测试框架搭建。

理由：当前阶段优先做通主链路，不做超前投入。

---

## 5. 失败处理规则

1. 任一主流程失败，停止新增功能。
2. 先修复再继续。
3. 连续两次失败同一问题，补一条防回归测试记录。
