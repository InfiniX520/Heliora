# Heliora(曦澪) 当前交付清单

本文只记录当前阶段真正要交付的内容，不写模板项，不写团队分工。

---

## 1. 当前必须交付

1. 可运行的最小系统（插件 + 后端 + 桌面端）。
2. 引导式纠错闭环可演示。
3. 图谱最小链路可查询。
4. 答辩演练最小版可生成反馈。
5. 文件管家安全闭环可回滚。
6. 长期记忆闭环可验证（写入、检索、冲突、回滚、删除SLA）。
7. 记忆专项测试报告可追踪（Recall@K、Precision@K、冲突准确率、删除SLA）。

---

## 2. 交付目录（按当前结构）

```text
Heliora/
  Heliora项目方案.md
  项目设计/
    01-战略与需求/
    02-架构与设计/
    03-交互与界面/
    04-技术规范/
    05-实施与交付/
      04-开发环境与实现启动清单.md
      05-发布窗口切流执行单.md
  NagaAgent-main/   # 仅参考，不作为交付代码
  参考设计规范/      # 仅参考，不作为交付代码
```

说明：
1. 交付代码必须在 Heliora 独立实现中体现。
2. 参考目录只保留思路，不计入功能完成度。
3. 开发执行以“04-开发环境与实现启动清单.md”为日常落地基线。

---

## 3. 演示必须覆盖的 7 个场景

1. 插件发送错误上下文。
2. 系统给出引导式定位与下一步建议。
3. 错误关联到知识点。
4. 生成一次答辩演练反馈。
5. 文件整理执行后可回滚。
6. 记忆检索命中并展示证据来源。
7. 触发冲突后可完成判别并支持删除闭环。

---

## 4. 当前不做

1. 复杂部署脚本与云端生产化。
2. 大规模压测与大样本评测。
3. 角色化展示与娱乐化模块。

---

## 5. 验收标准（直接判定）

满足以下条件即视为本阶段完成：
1. 七个演示场景可连续跑通。
2. 任一场景失败时可定位日志。
3. 文件操作可回滚，不破坏原始数据。
4. 删除请求闭环SLA <= 5分钟。

不满足则继续修复，不进入下一阶段。

---

## 6. Day-4.2 任务持久化交付证据（2026-04-04）

当前已补齐 PostgreSQL 运行时联调证据，作为本阶段交付附件：

1. CI 门禁证据：
  - `.github/workflows/backend-rabbitmq-gate.yml` 已包含 PostgreSQL 冒烟门禁。
  - 同一工作流已包含 RabbitMQ + PostgreSQL 组合门禁。
2. VM 实测证据：
  - `TASK_PERSISTENCE_BACKEND=postgres` + `TASK_QUEUE_BACKEND=memory`：`tests/test_tasks_submit.py` 27/27 通过。
  - `TASK_PERSISTENCE_BACKEND=postgres` + `TASK_QUEUE_BACKEND=rabbitmq`：`tests/test_tasks_rabbitmq_e2e.py` 1/1 通过。
3. 评审证据入口：
  - `heliora_backend/sql/task_persistence_pg/VM_VALIDATION_2026-04-04.md`
  - `heliora_backend/sql/task_persistence_pg/JUDGE_DETAILED_EXPLANATION.md`
  - `heliora_backend/sql/task_persistence_pg/EXPERT_DESIGN_REVIEW.md`

说明：
1. 当前默认运行后端仍为 SQLite，用于保持兼容。
2. PostgreSQL 切流仍按灰度计划推进，不在本交付批次内直接切换生产默认值。

## 7. Day-4.3 发布治理交付（2026-04-05）

1. 发布窗口切流执行单：
  - `项目设计/05-实施与交付/05-发布窗口切流执行单.md`
2. CI 报告固化能力：
  - workflow 输出 `.ci-reports/`（JUnit + coverage XML）
  - workflow 上传 artifact `backend-rabbitmq-gate-reports`
  - workflow 输出门禁摘要表（GitHub Step Summary）
3. 演练自动化入口：
  - `heliora_backend/scripts/rehearse_task_persistence_cutover_rollback_vm.sh`

## 8. Day-4.4 工程可移植性治理交付（2026-04-05）

1. VM 脚本 SSH 路径去硬编码：
  - `heliora_backend/scripts/verify_task_persistence_pg_vm.sh`
  - `heliora_backend/scripts/run_task_persistence_pg_matrix_vm.sh`
  - `heliora_backend/scripts/rehearse_task_persistence_cutover_rollback_vm.sh`
2. 脚本能力说明：
  - 移除宿主机绝对路径默认值（不再绑定单机目录结构）
  - 优先 `command -v ssh` 自动发现，失败时回退常见 Git for Windows 路径
  - 保留 `SSH_BIN` 显式覆盖能力，便于差异化环境治理
3. 本地校验证据：
  - 宿主机使用 Git Bash 执行 `bash -n` 对 3 个脚本完成语法校验，结果通过

## 9. Day-4.4 类型规范治理交付（2026-04-05）

1. ANN 注解治理范围：
  - `heliora_backend/app/`
  - `heliora_backend/tests/`
  - `heliora_backend/scripts/`
2. 治理结果：
  - `ruff check app tests scripts --select ANN` 全通过
  - `ruff check app tests` 全通过
  - `mypy app` 全通过
3. 说明：
  - 本批仅收敛类型注解债务与测试辅助签名，未变更任务编排与持久化业务语义

## 10. Day-4.4/Day-4.5 CI 质量治理交付（2026-04-05）

1. workflow 已完成 ANN 审计升级（阻塞门禁）：
  - `.github/workflows/backend-rabbitmq-gate.yml`
2. 审计能力说明：
  - 执行命令：`ruff check app tests scripts --select ANN`
  - 结果落盘：`.ci-reports/ann_audit.txt`
  - 结果展示：GitHub Step Summary 输出 `ann audit (blocking)` 行
3. 治理策略：
  - Day-4.4 已完成非阻塞观察
  - Day-4.5 在第二窗口稳定后升级为阻塞门禁，防止 ANN 债务回流

## 11. Day-4.4 发布前自动化治理交付（2026-04-05）

1. 一键预检脚本：
  - `heliora_backend/scripts/preflight_release_window_vm.sh`
2. 能力说明：
  - 串联 env consistency、VM SQL 评审、VM 矩阵回归、切换回滚演练
  - 自动输出预检报告到 `.release-reports/release_preflight_*.md`
  - 支持跳过重步骤的轻量模式（`--skip-sql-review --skip-matrix --skip-rehearsal`）
3. 校验证据：
  - 脚本语法校验通过（bash -n）
  - 轻量模式实跑通过并生成报告

## 12. Day-4.4 发布执行辅助交付（2026-04-05）

1. 无 git/无 gh 环境操作卡：
  - `项目设计/05-实施与交付/06-无Git仓库与无GH客户端-上传与触发CI操作卡.md`
2. 覆盖能力：
  - 本地 git 初始化与首次提交
  - GitHub 网页创建仓库与远程推送
  - PR 创建与网页触发 CI
  - CI 运行链接与 artifact 链接回填到执行单
3. 场景价值：
  - 解决当前环境无 git 仓库、无 gh 客户端时无法触发云端门禁的问题

## 13. Day-4.4 云端 CI 证据闭环交付（2026-04-05）

1. workflow 云端运行证据：
  - run#1（failure）：https://github.com/InfiniX520/Heliora/actions/runs/23993110943
  - run#2（success）：https://github.com/InfiniX520/Heliora/actions/runs/23993156057
  - run#3（success）：https://github.com/InfiniX520/Heliora/actions/runs/23993195493
2. artifact 证据：
  - backend-rabbitmq-gate-reports（id 6274768392）
  - 页面链接：https://github.com/InfiniX520/Heliora/actions/runs/23993195493/artifacts/6274768392
3. 闭环结论：
  - RabbitMQ E2E gate 与 artifact 上传问题均已修复并通过复验
  - 发布窗口执行单 8.1 已完成链接回填，当前结论为 Go（允许进入切流执行阶段）

## 14. Day-4.4 发布证据索引归档交付（2026-04-05）

1. 证据索引文档：
  - `项目设计/05-实施与交付/07-发布证据索引与检索指南.md`
2. 归档内容：
  - 发布时间线（含 run#1/run#2/run#3 演进）
  - 本地终端复核命令、执行目录、退出码
  - VM 报告路径与云端 artifact 链接
  - 本地部署与云端测试边界说明（FAQ）
3. 场景价值：
  - 后续发布窗口可直接复用模板，减少证据散落与重复沟通成本
