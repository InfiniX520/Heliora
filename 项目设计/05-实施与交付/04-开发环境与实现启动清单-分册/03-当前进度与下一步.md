# 当前进度与下一步

> 更新时间: 2026-04-05
> 来源: [04-开发环境与实现启动清单](../04-开发环境与实现启动清单.md)
> 目的: 独立维护状态追踪，避免和环境说明、实操步骤混杂。

---

## 8.1 最新进度（2026-04-05）

已完成:
1. Day-0 已完成：基础容器已稳定运行，健康检查与冒烟验证通过。
2. Day-1 已完成：`/api/v1/chat`、`/api/v1/memory/retrieve`、`/api/v1/tasks/submit` 三个骨架路由可用。
3. Day-1 已完成：有副作用接口已强制 `Idempotency-Key`，并实现同键同体幂等重放、同键异体冲突拦截（409）。
4. 下一步开发已启动并完成首项：新增 `/api/v1/tasks/{task_id}` 任务状态查询接口，提交后可查询状态。
5. Day-1 增量已完成：`/api/v1/chat` 已升级为最小真实流程（意图判定、动作建议、会话 turn_index、失败分支）。
6. Day-1 增量已完成：`/api/v1/memory/retrieve` 已升级为最小真实检索链（规则检索、scope/top_k 过滤、injected_context、失败分支）。
7. Day-2 最小闭环已完成：新增任务消费接口 `POST /api/v1/tasks/consume-next`。
8. Day-2 最小闭环已完成：新增任务事件查询接口 `GET /api/v1/tasks/{task_id}/events`。
9. Day-2 最小闭环已完成：状态推进已跑通 `queued -> running -> completed/failed`，并产出 `created/queued/running/completed/failed` 事件流。
10. Day-2 增量已完成：任务队列后端支持 `memory/rabbitmq` 切换（配置驱动）。
11. Day-2 增量已完成：失败任务已接入最小重试与死信处理（`requeued/dead_lettered`）。
12. Day-3 已完成：独立后台 worker 已接入，支持持续消费，不再依赖手动调用 `consume-next`。
13. Day-3 已完成：`start_api_bg.sh` 与 `stop_api.sh` 已联动 worker 启停。
14. Day-3 热修复已完成：修复 worker 启动时 `.env`（CRLF + 空格值）解析失败问题。
15. Day-3.2 已完成：虚拟机实机复测通过，`check_services.sh` 显示 `worker running`，`.worker.log` 持续消费循环正常。
16. Day-3.3 已完成：指数退避策略已接入重试链路（base/factor/max）。
17. Day-3.3 已完成：RabbitMQ 联调验收脚本已新增（`smoke_rabbitmq_retry.sh`）。
18. Day-3.3 已完成：`task_events` 持久化已接入 SQLite，事件端点支持持久层回读。
19. 自动化测试已通过：当前 `pytest` 结果 `55 passed, 1 skipped`（本地 RabbitMQ 不可用时 E2E 用例按非强制模式跳过）。
20. Day-3.3.1 已完成：联调脚本兼容 `python3` 并新增 backend 严格校验，避免 memory fallback 假阳性。
21. Day-3.3.2 已完成：虚拟机 RabbitMQ 联调实机通过（`requeued/dead_lettered` 均为 `backend=rabbitmq`）。
22. Day-3.4 第一批已完成：任务状态机对齐 `retrying`，补齐 `routed` 事件与任务取消接口。
23. Day-3.4 第一批已完成：任务不存在错误码对齐为 `TASK_NOT_FOUND`，安全模式补齐 loopback 约束。
24. Day-3.4 第一批已完成：RabbitMQ 重试改为 TTL + dead-letter 延迟投递（非阻塞 worker）。
25. Day-3.4 第二批已完成：`task_events` 查询已支持 `event_type/start_ts/end_ts/limit/offset` 过滤与分页。
26. Day-3.4 第二批已完成：RabbitMQ 模式端到端自动化测试已接入（环境不可用时 `skip`）。
27. Day-3.5 治理加固已完成：任务状态迁移改为中心化 allowed-transition 校验，拒绝非法迁移。
28. Day-3.5 治理加固已完成：`TASK_QUEUE_FAIL_OPEN` 仅对可恢复的 RabbitMQ 连接/通道异常生效。
29. Day-3.5 治理加固已完成：`/tasks/{task_id}/events` 时间参数改为 ISO-8601 严格校验并统一 UTC 语义。
30. Day-3.6 已完成：任务主状态表持久化已接入 SQLite，`/tasks/{task_id}` 支持持久层回读。
31. Day-3.7 已完成：任务路由策略与 SLA 已下沉到服务层并配置化，API 层仅负责调用路由结果。
32. Day-3.8 已完成：新增 CI 工作流并接入 RabbitMQ 服务，`tests/test_tasks_rabbitmq_e2e.py` 在 CI 中强制通过（不可 skip）。
33. Day-3.9 已完成：CI 质量门禁已补齐，新增 Ruff 静态检查、Mypy 类型检查、覆盖率阈值（`app` 覆盖率 >= 70%）。
34. Day-3.9 已完成：本地质量门禁命令已纳入执行基线，作为合并前固定检查项。
35. Day-4 设计已完成：任务持久化 PostgreSQL 详细 SQL 方案已产出（`task_registry/task_events` + up/down/verify），当前处于评审与检测阶段，尚未接入运行时。
36. Day-4 设计增强已完成：补充专家评审包（traceability 设计文档 + assertion 评审 SQL 套件 + 评审入口说明），可直接用于跨团队评审。
37. Day-4 口径回归已完成：状态机重新对齐统一基线（`failed` 终态、取消仅 `queued/running`），并同步修订 SQL 验证脚本、专家套件、评审文档与后端运行时常量。
38. Day-4 验证自动化已完成：新增主机侧 VM 校验脚本 `scripts/verify_task_persistence_pg_vm.sh`，可通过 SSH 一键执行 up/verify/expert/down 全套 SQL 评审流程。
39. Day-4 VM 验证脚本增强已完成：支持远端目录 `~/heliora_backend` 自动识别，并在 VM 缺少 `psql` 时自动回退到 `docker exec heliora-postgres` 执行 SQL 验证链路。
40. Day-4 运行时接入第一步已完成：任务状态与事件持久层支持 `sqlite/postgres` 后端切换（配置键 `TASK_PERSISTENCE_BACKEND` + DSN 回退到 `DATABASE_URL`），默认仍为 `sqlite` 保持兼容。
41. Day-4 重启复验已完成：虚拟机重启后再次执行 `scripts/verify_task_persistence_pg_vm.sh` 全链路通过（up/verify/expert/down），验证流程具备重启后稳定性。
42. Day-4.2 第一批已完成：PostgreSQL 运行时冒烟在 VM 实机通过（`TASK_PERSISTENCE_BACKEND=postgres` + `TASK_QUEUE_BACKEND=memory`，`tests/test_tasks_submit.py` 27/27 passed）。
43. Day-4.2 第一批已完成：CI 工作流已新增 PostgreSQL service 与任务持久化冒烟门禁（建表 -> pytest -> 自动回滚）。
44. Day-4.2 第二批已完成：CI 新增 `RabbitMQ + PostgreSQL` 组合门禁（建表 -> `tests/test_tasks_rabbitmq_e2e.py` -> 自动回滚），覆盖队列与持久层联动路径。
45. Day-4.2 第二批已完成：修复 PostgreSQL 持久化隐性失败（状态先落库再写事件、`created_at` 显式入库、append-only 清理降级策略），并在 VM 组合回归中通过。
46. Day-4.2 第二批已完成：RabbitMQ E2E 用例补充持久层断言（强制校验 `task_state_store/task_event_store` 写入成功）。
47. Day-4.2 第三批已完成：新增一键矩阵脚本 `scripts/run_task_persistence_pg_matrix_vm.sh`，自动执行 `postgres+memory` 与 `postgres+rabbitmq` 两条回归链路。
48. Day-4.2 第三批已完成：实机执行矩阵脚本通过（`tests/test_tasks_submit.py` 27/27 + `tests/test_tasks_rabbitmq_e2e.py` 1/1）。
49. Day-4.3 第一批已完成：新增切换与回滚演练脚本 `scripts/rehearse_task_persistence_cutover_rollback_vm.sh`，按一条命令执行 postgres 切换矩阵 + sqlite 回滚验证。
50. Day-4.3 第一批已完成：实机执行演练脚本通过（postgres+memory 27/27 + postgres+rabbitmq 1/1 + sqlite 回滚 27/27）。
51. Day-4.3 第二批已完成：新增环境一致性校验脚本 `scripts/validate_env_consistency.py`，校验 `DATABASE_URL/POSTGRES_*` 与 `RABBITMQ_URL/RABBITMQ_DEFAULT_*` 的一致性。
52. Day-4.3 第二批已完成：CI 与 VM 矩阵脚本接入环境一致性预检，实机复跑矩阵通过（含预检通过日志）。
53. Day-4.3 第三批已完成：CI 工作流新增报告固化能力（JUnit/coverage XML 产物 + artifact 上传 + 门禁摘要表）。
54. Day-4.3 第三批已完成：新增发布窗口切流执行单 `05-发布窗口切流执行单.md`，固化 Go/No-Go 与回滚判定标准。
55. Day-4.4 第一批已完成：三套 VM 脚本移除 SSH 绝对路径默认值，改为自动发现策略（`command -v ssh` + Git for Windows 常见路径兜底），并保留 `SSH_BIN` 显式覆盖能力。
56. Day-4.4 第一批已完成：宿主机已对三套 VM 脚本执行 `bash -n` 语法校验并通过。
57. Day-4.4 第二批已完成：ANN 类型注解治理已覆盖 `app/tests/scripts`，本地扫描结果 `ruff check app tests scripts --select ANN` 全通过。
58. Day-4.4 第二批已完成：类型与静态基线复测通过（`ruff check app tests` 与 `mypy app` 全通过）。
59. Day-4.4 第三批已完成：CI 工作流 `backend-rabbitmq-gate.yml` 已接入 ANN 非阻塞审计（`ruff check app tests scripts --select ANN`），并将结果写入 `.ci-reports/ann_audit.txt` 与 Step Summary。
60. Day-4.4 第四批已完成：新增发布前一键预检脚本 `scripts/preflight_release_window_vm.sh`，串联环境一致性、VM SQL 评审、矩阵回归与切换回滚演练，并自动生成 `.release-reports/release_preflight_*.md` 报告。
61. Day-4.4 第四批已完成：轻量预检实跑通过（`--skip-sql-review --skip-matrix --skip-rehearsal`），验证脚本在当前宿主机可用。
62. Day-4.4 第五批已完成：全量预检实跑通过（`bash scripts/preflight_release_window_vm.sh`），报告 `heliora_backend/.release-reports/release_preflight_20260405_101918.md` 显示四步均 PASS。
63. Day-4.4 第五批已完成：发布执行单已回填本批次证据并给出阶段判定 `No-Go`（待补 PR 云端 CI 运行链接与 artifact 链接，已在第十批闭环为 Go）。
64. Day-4.4 第六批已完成：补充新一轮全量预检证据 `release_preflight_20260405_101918.md`，并归档矩阵与演练日志（`run_task_persistence_pg_matrix_vm.md`、`rehearse_task_persistence_cutover_rollback_vm.md`）。
65. Day-4.4 第六批已完成：发布执行单补充 Windows 环境执行说明，解决 PowerShell 根目录执行与 Git Bash `python/conda` 可用性差异问题。
66. Day-4.4 第七批已完成：新增 `06-无Git仓库与无GH客户端-上传与触发CI操作卡.md`，固化当前环境下从上传到触发 CI 再到证据回填的完整操作流程。
67. Day-4.4 第八批已完成：首次云端 CI（run#1）已在 `master` 触发，运行链接 `https://github.com/InfiniX520/Heliora/actions/runs/23993110943`，结论 failure（RabbitMQ E2E gate）。
68. Day-4.4 第八批已完成：已修复 workflow 中 RabbitMQ E2E 步骤命令折行写法（避免 pytest 用法错误），待二次触发云端 CI 复验。
69. Day-4.4 第九批已完成：run#2 复验通过（`https://github.com/InfiniX520/Heliora/actions/runs/23993156057`），确认 RabbitMQ E2E gate 修复生效。
70. Day-4.4 第九批已完成：修复 artifact 上传隐藏目录问题后，run#3 通过并生成 artifact（`https://github.com/InfiniX520/Heliora/actions/runs/23993195493`，artifact id `6274768392`）。
71. Day-4.4 第十批已完成：发布执行单已完成 CI 链接与 artifact 链接回填，当前结论更新为 `Go（允许进入切流执行阶段）`。
72. Day-4.4 第十一批已完成：新增证据归档文档 `07-发布证据索引与检索指南.md`，集中沉淀 run/artifact/命令/CWD/退出码与结论，提升后续检索效率。
73. Day-4.4 第十一批已完成：补录同日终端复核结果（`python validate_env_consistency.py --env-file .env` 于 `heliora_backend/scripts` 执行 exit code 0；run#3 轮询确认 `completed/success` 且 `artifact_count=1`）。
74. Day-4.4 第十二批已完成：Git 配置与推送实操过程已文档化（含 `Author identity unknown`、GH007、`--reset-author`、`push --force` 处理链路），并已推送远端 `master`。
75. Day-4.4 第十二批已完成：复核 workflow 路径过滤规则后确认，文档-only push 不触发 `Backend RabbitMQ Gate` 新 run 属预期；门禁复测需 `Re-run all jobs` 或提交命中 `heliora_backend/**` 的改动。
76. Day-4.4 第十三批已完成：`validate_env_consistency.py` 增加 `.env` 路径自动回退（当 `scripts/.env` 不存在时自动尝试 `heliora_backend/.env`），解决在 `heliora_backend/scripts` 目录执行 `--env-file .env` 的误报错。
77. Day-4.4 第十三批已完成：脚本双场景实测通过（在 `heliora_backend/scripts` 与 `heliora_backend` 目录执行 `--env-file .env` 均 PASS）。
78. Day-4.4 第十三批已完成：脚本修复提交后触发云端门禁 run#4（`https://github.com/InfiniX520/Heliora/actions/runs/23993634248`）并成功，artifact id `6274898630` 已回填执行单与证据索引。
79. Day-4.4 第十四批已完成：VM 执行 T0 切流，将 `TASK_PERSISTENCE_BACKEND=postgres` 落盘到远端 `.env` 并完成 API/worker 重启。
80. Day-4.4 第十四批已完成：切流后端点 smoke 通过（submit/status/events/cancel + consume-next），证据报告 `heliora_backend/.release-reports/cutover_t0_smoke_vm_20260405_120029.md`。
81. Day-4.4 第十四批已完成：切流后双路径验证通过（postgres+memory: `tests/test_tasks_submit.py` 27 passed；postgres+rabbitmq: `tests/test_tasks_rabbitmq_e2e.py` 1 passed）。
82. Day-4.4 第十四批已完成：VM 运行态复核通过（`/health`=OK，worker running）。
83. Day-4.4 第十四批已完成：T0 证据回填后云端门禁 run#5（`https://github.com/InfiniX520/Heliora/actions/runs/23993871618`）复验成功，artifact id `6274973864`。
84. Day-4.4 第十五批已完成：观察窗口扫描通过（api/worker 错误签名命中均为 0，窗口 45m52s），报告 `heliora_backend/.release-reports/cutover_observation_window_vm_20260405_124617.md`。
85. Day-4.4 第十五批已完成：发布执行单已完成最终签字归档，当前口径更新为 `Go（已完成切流、观察窗口通过并签字归档）`。
86. Day-4.4 第十五批已完成：签字归档提交后云端门禁 run#6（`https://github.com/InfiniX520/Heliora/actions/runs/23994501682`）复验成功，artifact id `6275170352`。
87. Day-4.5 第一批已完成：第二发布窗口启动前轻量预检通过（`bash scripts/preflight_release_window_vm.sh --skip-sql-review --skip-matrix --skip-rehearsal`），报告 `heliora_backend/.release-reports/release_preflight_20260405_130437.md`。
88. Day-4.5 第一批已完成：云端门禁快照复核通过（截至 2026-04-05 13:04，本分支最近 5 次 backend gate 均为 `completed/success`）。
89. Day-4.5 第二批已完成：提交第二窗口预检证据后云端门禁 run#7（`https://github.com/InfiniX520/Heliora/actions/runs/23994760208`）复验成功，artifact id `6275265173`。

当前阶段锚点:
1. **Day-4.1（已完成）**：评审 SQL 包、状态机口径回归、VM 一键验证、运行时 `sqlite/postgres` 后端切换能力落地。
2. **Day-4.2（已完成）**：PostgreSQL 运行时联调与回归门禁三批落地（API smoke + CI 集成门禁 + RabbitMQ 组合门禁 + 一键矩阵回归）。
3. **Day-4.3（已完成）**：迁移与回滚演练自动化 + 环境一致性门禁 + CI 报告固化已落地，且云端 CI 实跑证据闭环完成。
4. **Day-4.4（已完成）**：工程可移植性与发布前治理、T0 切流、切流后 smoke、观察窗口与签字归档均已完成。
5. **Day-4.5（进行中）**：第二发布窗口已完成启动预检，待执行全量复核与观察窗口记录。

当前未完成:
1. 在云端 CI 稳定 2 个发布窗口后，将 ANN 审计从非阻塞升级为阻塞门禁。
2. 第二发布窗口全量复核（SQL 评审 + 矩阵 + 演练 + 观察窗口）尚未完成。

下一步（马上执行）:
1. 在第二发布窗口执行全量预检（不跳过 SQL review/matrix/rehearsal）并生成新报告。
2. 同窗口完成运行态观察并回填执行单、测试计划与证据索引。
3. 第二窗口稳定后，将 ANN 审计从非阻塞升级为阻塞门禁并同步 CI 文档口径。
4. 若后续观察出现任一回滚触发条件，立即按执行单第 7 节回滚并记录影响范围与恢复时间。
